# Tier 1 (unit) + Tier 2 (golden scenario) tests — deterministic, no API keys.
# Stub until v0.1 code lands.
name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Tier 1 + Tier 2 tests (placeholder)
        run: echo "Test suite lands with v0.1 — see README status."
